# Railway Environment Variables to Set
# Copy these to Railway Dashboard: Variables tab

# OpenAI API Key
OPENAI_API_KEY=your-openai-api-key-here

# Firebase Admin SDK (paste entire JSON content as single line)
FIREBASE_ADMIN_CREDENTIAL={"type":"service_account","project_id":"..."}

# App Configuration
HOST=0.0.0.0
PORT=$PORT

# Database URL (automatically set by Railway PostgreSQL addon)
# DATABASE_URL=postgresql://... (Railway sets this automatically)

# CORS Origins (update after frontend is deployed)
ALLOWED_ORIGINS=https://your-frontend-url.vercel.app,http://localhost:5173
